# 💊 Моніторинг ЄЛіки — Абіратерон

Автоматичний Telegram-бот, який відстежує наявність Абіратерону на сайті [ЄЛіки](https://eliky.in.ua/medicament/10986) і надсилає сповіщення при появі нових записів.

Працює **безкоштовно 24/7** через **GitHub Actions** — жодного сервера не потрібно.

---

## 🚀 Налаштування (5 хвилин)

### Крок 1 — Створіть репозиторій на GitHub

1. Зайдіть на [github.com](https://github.com) → **New repository**
2. Назва: `eliky-monitor` (або будь-яка інша)
3. Зробіть **Private** (щоб токени були в безпеці)
4. Натисніть **Create repository**

### Крок 2 — Завантажте файли

Завантажте всі файли цього архіву у ваш репозиторій. Структура має бути:

```
eliky-monitor/
├── .github/
│   └── workflows/
│       └── monitor.yml
├── monitor.py
├── requirements.txt
└── README.md
```

**Варіант A — через браузер (найпростіше):**
- У репозиторії натисніть **Add file → Upload files**
- Перетягніть усі файли
- Для папки `.github/workflows/monitor.yml` — створіть через **Add file → Create new file**, введіть шлях `.github/workflows/monitor.yml` і вставте вміст

**Варіант B — через термінал:**
```bash
git clone https://github.com/ВАШ_ЛОГІН/eliky-monitor.git
cd eliky-monitor
# скопіюйте сюди всі файли
git add .
git commit -m "Initial commit"
git push
```

### Крок 3 — Додайте секрети (токен і Chat ID)

1. У репозиторії → **Settings** → **Secrets and variables** → **Actions**
2. Натисніть **New repository secret** і додайте два секрети:

| Name | Value |
|------|-------|
| `TELEGRAM_TOKEN` | Токен вашого бота (від @BotFather) |
| `CHAT_ID` | ID чату або `@назва_каналу` |

> **Як отримати Chat ID:**  
> Напишіть боту `/start`, потім відкрийте у браузері:  
> `https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates`  
> Знайдіть `"chat":{"id": 123456789}` — це і є ваш Chat ID.

### Крок 4 — Перевірте роботу

1. Перейдіть у вкладку **Actions** репозиторію
2. Натисніть **Моніторинг ЄЛіки — Абіратерон** → **Run workflow** → **Run**
3. Через 30 секунд побачите результат. Якщо все ОК — у Telegram прийдуть повідомлення про всі наявні записи.

З наступного запуску — лише **нові** записи.

---

## ⏰ Розклад

Скрипт запускається **кожну годину** автоматично.  
Змінити розклад можна у файлі `.github/workflows/monitor.yml`:

```yaml
- cron: '0 * * * *'    # кожну годину
- cron: '0 */2 * * *'  # кожні 2 години  
- cron: '*/30 * * * *' # кожні 30 хвилин
```

> ⚠️ GitHub безкоштовно надає 2000 хв/місяць. Кожен запуск займає ~30 сек.  
> При перевірці **щогодини**: 24 × 30сек × 30 днів = **360 хв/міс** — добре вкладається в ліміт.

---

## 📨 Приклад повідомлення

```
💊 АБІРАТЕРОН — новий запис

🏥 Лікарня: Криворізький онкологічний диспансер
📍 Область: Дніпропетровська
💊 Кількість: 7560 таб.
📅 Дата оновлення: 24.02.2026

🔗 Переглянути на ЄЛіки
```

---

## 🔧 Як це працює

1. GitHub Actions запускає `monitor.py` кожну годину
2. Скрипт завантажує сторінку і парсить таблицю
3. Порівнює з `state.json` (кешується між запусками)
4. Нові записи → Telegram-повідомлення
5. Оновлює кеш — дублікатів не буде
